# 下一步（你只需这3步）

1. 用 STM32CubeIDE 打开 `CurtainMVP_F103.ioc` 并点击 **Generate Code**
2. 在工程根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File D:\codex\projectB\firmware\setup_projectb_stm32.ps1 -ProjectRoot "<你的Cube工程目录>"
```

3. 编译下载，串口看 `boot ok`

---

如果你在这一步报错，把报错贴给我，我继续帮你改到能跑。

